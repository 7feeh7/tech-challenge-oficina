apiVersion: v1
kind: ServiceAccount
metadata:
  name: oficina-api
  namespace: oficina
  labels:
    app: oficina-api
  annotations:
    # Substituído pelo pipeline com o ARN exportado via SSM (IRSA).
    eks.amazonaws.com/role-arn: API_NOTIFICACAO_IRSA_ROLE_ARN
